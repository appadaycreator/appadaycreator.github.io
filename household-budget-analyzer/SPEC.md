# 家計診断【無料】毎月いくら節約できる？収支分析ツール - 技術仕様書

## 概要

**サービス名**: Household Budget Analyzer
**バージョン**: 1.9.0
**更新日**: 2026-06-13
**URL**: https://appadaycreator.com/household-budget-analyzer/

月収・支出を入力するだけで家計の問題点を自動分析。節約できる項目と金額を具体的に提案。総務省統計データに基づく理想比率と比較し、家計改善の優先順位が一目でわかる無料ツール。

## v1.9.0 変更点（2026-06-13）

### P1改善
- **メタディスクリプション最適化**（SEO キーワード強化。「無料」「診断」「収支分析」などの重要キーワードを先頭に配置。GSC 表示回数改善を狙う）
- **FAQPage スキーマ拡張（2問 → 5問）**（「何を診断できるか」「結果の活用方法」「データ保存場所」「月収変更時の対応」「複数人診断」の5問に拡張。Google FAQ パック表示獲得を狙う）
- **HowTo スキーマ拡張（3ステップ → 5ステップ）**（「月収入力」「家族構成・地域選択」「支出入力」「診断実行」「結果確認」の5ステップに拡張。Google How-To リッチスニペット最適化）
- **汎用コンテンツセクション削除**（「r31-tool-about」から「AppADayCreatorでは、日常生活のあらゆるシーンで役立つWebツール...」の汎用文言を削除。AdSense「スケールされたコンテンツ悪用」ポリシー違反防止）
- **空の FAQ セクション削除**（「r31-extended-faq」セクション全体を削除。ページ体積削減・検索エンジンノイズ排除）

## v1.8.1 変更点（2026-06-10）

### P1改善
- **OGタグ・Twitterカード追加**（SEO・SNSシェア最適化。`og:title`・`og:description`・`og:image`・`og:url`・`og:type`・`og:locale` および `twitter:card`・`twitter:title`・`twitter:description`・`twitter:image` を head セクションに追加。SNSシェア時のプレビュー表示・クリック率向上）

## v1.8.0 変更点（2026-06-07）

### P1改善
- **プレビューバー初期表示バグ修正**（`DOMContentLoaded` 末尾に `updatePreview()` を追加。ページ読み込み直後に「-」が表示されていた問題を解消し、入力前はバーを非表示にする）
- **「その他」費超過時の改善提案欠落バグ修正**（`generateRecommendations()` に `others` カテゴリのチェックを追加。偏差+3%超でサブスク整理・固定費見直しアドバイスを生成。`actionPlanTemplates` にも「その他費」週次テンプレートを追加）

### P2改善
- **月収入力後に各支出欄へ理想金額ヒントをリアルタイム表示**（`DOMContentLoaded` で `<small id="hint-{field}">` 要素を動的注入。`updatePreview()` で月収・家族タイプに応じた「理想: ¥XX,XXX（月収のX%）」を表示。家族タイプ変更時も即時更新）
- **収支マイナス時の緊急アラートをプレビューバーに追加**（収支がマイナスになった瞬間にプレビューバーを赤背景に変更し「⚠️ 支出が月収を超えています！」を表示。プラスに戻ったら通常スタイルに復帰）

## v1.7.0 変更点（2026-06-06）

### P1改善
- **resetFormのinsurance/medical欠落バグ修正**（`resetForm()` のフィールドリストに `insurance`・`medical` を追加。リセット時に保険料・医療費が残留する不具合を解消）
- **利用者の声を家計診断関連に修正**（「賃貸vs購入」「引越し費用」「リフォーム費用感」という別サービス向けテスティモニアルを食費節約・格安SIM切替・貯蓄率改善の体験談に差し替え）
- **startMonthlyCheck()のスクロール修正**（`querySelector("form, .input-section, ...")` → `getElementById('input-section')` に変更。「今月の診断を始める」ボタン押下で入力フォームへ正常にスクロールするよう修正）

### P2改善
- **モバイルで支出入力を2列グリッド化**（9フィールドが縦1列だったのを `grid-cols-2` で2列に変更。デスクトップは `md:block` でスタック表示を維持。スクロール量を約半分に削減）
- **プレビューバーをスティッキー表示**（`position: sticky; bottom: 0; z-index: 20` を追加。フォーム入力中にスクロールしても画面下部に収支バランス・貯蓄率が常時表示されるよう改善）

## v1.6.0 変更点（2026-06-06）

### P1改善
- **Tailwind CSS 動的クラス生成を修正**（推奨事項セクションの色分けが正確に反映されるようにインラインスタイルに切り替え）
- **履歴削除時の確認ダイアログを追加**（`clearHistory()` / `deleteHistoryItem()` で「本当に削除しますか？」確認を表示）
- **チャート破棄エラーを修正**（初回描画時の `charts.destroy()` undefined エラーを suppress）

### P2改善
- **リアルタイムプレビューバーを常時表示**（月収・支出入力時に即座に「収支バランス・貯蓄率」を表示）
- **目標貯蓄額のバリデーション強化**（月収を超える目標額を入力した場合に警告表示）
- **スクリーンリーダー対応を強化**（カテゴリ別比較・詳細分析セクションに `role="region"` + `aria-label` を追加）

## v1.5.6 変更点（2026-06-05）

- **P1**: スティッキー広告・履歴削除ボタンにaria-labelを追加（スティッキー広告のクローズボタン✕に`aria-label="広告を閉じる"`、診断履歴の削除ボタン🗑️に`aria-label="この診断結果を削除する"`を設定。WCAG 2.1 Level AAアクセシビリティ改善）

## v1.5.5 変更点（2026-06-05）

- **P1**: LINEシェアボタンとコピーボタンにaria-labelを追加（LINEボタンに`aria-label="診断結果をLINEでシェアする"`、コピーボタンに`aria-label="診断結果をクリップボードにコピーする"`を設定。WCAG 2.1 Level AAアクセシビリティ改善）

## v1.5.4 変更点（2026-06-05）

- **P1**: TwitterシェアボタンとFacebookシェアボタンにaria-labelを追加（TwitterボタンとFacebookボタンに`aria-label`属性を設定してスクリーンリーダー対応を改善。WCAG 2.1 Level AAアクセシビリティ改善）

## v1.5.3 変更点（2026-06-04）

- **P1**: 履歴削除・前回結果表示ボタンにaria-labelを追加（「履歴をすべて削除」ボタンに`aria-label="診断履歴をすべて削除する"`、「前回の結果を見る」ボタンに`aria-label="前回の診断結果セクションまでスクロールして表示する"`を設定。WCAG 2.1 Level AAアクセシビリティ改善）

## v1.5.2 変更点（2026-06-04）

- **P1**: シェアボタン4つにaria-labelを追加（Twitterシェア・Facebookシェア・LINEシェア・コピー機能それぞれに`aria-label`属性を設定。WCAG 2.1 Level AAアクセシビリティ改善）

## v1.5.1 変更点（2026-06-03）

- **P1**: メインボタン2つにaria-labelを追加（「家計診断を実行」ボタンに`aria-label="月収と支出の情報から家計診断を実行する"`、「リセット」ボタンに`aria-label="入力フォームをリセットして初期状態に戻す"`を設定。WCAG 2.1 Level AAアクセシビリティ改善）

## データ管理

- **ストレージ**: ブラウザ localStorage（外部API通信なし）
- **永続化**: ページ読み込み時に自動復元
- **クリア**: ブラウザのサイトデータ削除で初期化

## 技術スタック

- HTML5 / CSS3 / Vanilla JavaScript
- PWA対応（manifest.json / Service Worker）
- レスポンシブデザイン（モバイルファースト）

## 使い方

1. 記録したい項目を入力フォームに入力する
2. 「追加」または「記録」ボタンをクリックする
3. 記録一覧で過去のデータを確認する
4. グラフや集計で傾向を把握する
5. 継続的に記録して変化を追跡する

## よくある質問（FAQ）

**Q: 家計診断は無料で使えますか？**

はい、完全無料・登録不要でご利用いただけます。

**Q: 記録データはどこに保存されますか？**

ご使用のブラウザのローカルストレージに保存されます。他のデバイスとは共有されません。

**Q: ブラウザを閉じても記録は残りますか？**

はい、同じブラウザを使う限りデータは保持されます。

**Q: 記録を削除するにはどうすればいいですか？**

リセットボタン、またはブラウザのサイトデータをクリアすることで削除できます。

**Q: スマートフォンで利用できますか？**

はい、スマートフォン・タブレット・PCすべてでご利用いただけます。


## 関連サービス

- [睡眠の質チェッカー](https://appadaycreator.com/sleep-quality-checker/)
- [BMI・体重管理](https://appadaycreator.com/bmi-body-tracker/)
- [家計簿診断](https://appadaycreator.com/household-budget-analyzer/)

## v1.5.0 変更点（2026-05-31）

- **P1**: 保険料・医療費カテゴリを支出入力に追加（`idealBudgetRatios` 全家族タイプに比率追加、`generateRecommendations` でチェック・アドバイス生成、`getCategoryName`/`loadHistoryItem`/`updatePreview` に反映）
- **P1**: 月次トレンドグラフを履歴セクションに追加（2件以上の履歴がある場合に貯蓄率・収支バランスの折れ線グラフを表示。`renderTrendChart()` 新設・`displayHistory()` から呼び出し）
- **P2**: 交通費・水道光熱費・保険料・医療費の30日アクションプランテンプレート追加（`actionPlanTemplates` に4カテゴリ追加。交通費・水道光熱費の超過チェックも `generateRecommendations` に追加）
- **P3**: 年間節約シミュレーションカードを診断結果に追加（節約ポテンシャルの年間額・3年後・5年後の複利計算結果を3枚カードで表示。`displaySavingsProjection()` 新設）
- **P4**: サンプルデータ投入ボタンをフォームヘッダーに追加（「30代単身」「40代夫婦+子1」の2パターン。クリックで全フィールドを設定しプレビューバーを即座に更新する `fillSample()` 関数追加）

## v1.4.0 変更点（2026-05-30）

- **P1**: カテゴリ別比較バーの超過閾値を分析ロジックと統一（`+1%` → `+2%`。`performDetailedAnalysis` のリスク判定と同じ閾値にすることでバー色分けとリスクエリア表示の不整合を解消）
- **P2**: 推奨事項の優先度を節約可能額ベースに動的化（月収の3%以上の節約が見込める項目を「高」優先度に自動昇格、`potentialSaving` 降順ソートで重要項目を先頭表示）
- **P2**: バリデーション関数 `validateInput()` を分離・強化（月収未入力・支出合計超過など7種類の具体的エラーメッセージ、エラー時に月収フィールドへフォーカス）
- **P3**: 目標貯蓄額UIを強調表示（背景色・ボーダー付きカードに変更、「入力すると不足額と削減費目を自動計算」の説明文追加、プレースホルダーに具体的な例を表示）
- **P4**: モバイルグラフ凡例フォントサイズ改善（10px → 12px。WCAG 2.1 AA基準の最小フォントサイズに合わせて視認性向上）
- **P5**: アクセシビリティ改善（結果セクションに `role="region" aria-label="診断結果"` 追加、`aria-live="polite"` live regionで診断完了を自動通知、診断ボタンに `aria-describedby` 追加）

## v1.3.0 変更点（2026-05-29）

- **P1**: 30日実行プランを動的生成に変更（診断結果の高優先度推奨項目に基づいたカテゴリ別週次プランを表示。改善不要時は資産形成フェーズの特別プランを表示）
- **P1**: 0円カテゴリを円グラフから除外（未入力項目が凡例に表示されないようフィルタリング追加）
- **P1**: フォームリセットボタン追加（全フィールドを一括クリアしプレビューバーを非表示にする `resetForm()` 関数）
- **P2**: 家計健全度スコア追加（貯蓄率・リスクエリア数・最大超過率から100点満点スコアとA〜Eグレードを算出し4枚目のサマリーカードに表示）
- **P2**: 支出カテゴリ別 現在vs理想 比較バー追加（全7カテゴリをプログレスバーで視覚化、超過は赤・理想内は緑で色分け）
- **P2**: 履歴「再表示」でフォームの値を復元（`loadHistoryItem()` に入力フォーム・プレビューバー更新処理を追加）
- **P3**: 円グラフ凡例のモバイル最適化（640px以下では凡例を下部配置・フォントサイズ縮小）
- **P3**: 目標貯蓄額の逆算機能追加（任意入力の目標月間貯蓄額から不足額と推奨削減カテゴリを計算・表示）

## v1.2.0 変更点（2026-05-29）

- **P1**: PWA theme-color を全ファイルで `#7c3aed` に統一（manifest.json + 全サブページ）
- **P1**: Service Worker（sw.js）を正しく実装。Blob URLからの動的登録を廃止し `sw.js` を直接登録
- **P2**: 月次チェック「診断済み」メッセージの演算子優先順位バグ修正（`d.getMonth()+1` → `(d.getMonth()+1)`）
- **P2**: 「前回の結果を見る」ボタン修正（存在しないセレクタから `history-section` へのスクロールに変更）
- **P2**: リアルタイムプレビューバー追加（収支バランス・貯蓄率・支出合計をフォーム入力中に表示）
- **P3**: 印刷CSS改善（チャート高さ調整・ページ区切り・グラデーション印刷対応）
- **P3**: 全サブページのヘッダーグラデーションカラーを `#7c3aed` に統一

## テスト

| ファイル | フレームワーク | 概要 |
|---------|--------------|------|
| `tests/e2e/` | Playwright | 本番URL対象E2E（Jest対象外） |

## デプロイ

GitHub Pages（mainブランチ push → 自動デプロイ）

## ライセンス

MIT License
