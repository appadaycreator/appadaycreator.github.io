# 室内湿度・空気環境最適化診断【無料】無料診断・アドバイス - 技術仕様書

## 概要

**サービス名**: Humidity Home Advisor
**バージョン**: 1.3.0
**更新日**: 2026-06-10
**URL**: https://appadaycreator.com/humidity-home-advisor/

季節・住宅タイプ・症状から最適な室内湿度と加湿器・除湿器の選び方・使い方を診断。登録不要・完全無料でご利用いただけます。

## 変更履歴

### v1.4.0 (2026-06-14)
- **M1: 診断/計算後の文脈CTAボタン設置**: 診断結果表示直後に結果タイプ別のアフィリエイトCTAボタンを自動表示
  - 加湿器診断結果 → 「加湿器を探す（Amazon）」ボタン
  - 除湿器診断結果 → 「除湿器を探す」ボタン（以下同様）
  - px.a8.net アフィリエイトリンク保持・A8.netボタンも併配置
  - 結果詳細セクション内 CTA領域に自動表示
- **M7: 比較・シミュレーション機能**: 複数パターンの入力値を並べて比較・即時再計算
  - 「もし〜だったら診断結果は？」セクション追加
  - 季節・症状を選択して「実行」ボタンで即座にシミュレーション結果を表示
  - 結果スコアを4つのタイプ別に縦棒グラフで可視化
  - 条件を変更するたびに即座に結果が再計算される
- **GSC SEO最適化**: インデックス流入改善のための構造化データ・SEOキーワード強化
  - メタタイトル最適化: 「室内湿度診断・加湿器除湿器選び方【無料】快適空気環境最適化」（SEOキーワード追加）
  - メタ description 拡張: 「加湿器・除湿器・空気清浄機・サーキュレーター選び方」「カビ・結露・乾燥・アレルギー対策」等のキーワード追加
  - FAQPage スキーマ拡張: 2問から5問に増加（加湿除湿の使い分け・データ保存・診断回数等）
  - Organization スキーマ新規追加: AppADayCreator ブランド情報・SNS リンク
  - OGP description キーワード強化: SNSシェア時の検索キーワード最適化
  - HowTo スキーマ description 充実: 「カビ・結露・乾燥・アレルギー対策」等の目的語追加

### v1.3.0 (2026-06-10)
- **M3: データ保存・履歴機能**: localStorage に診断結果を自動保存。履歴一覧表示・復元・削除機能を実装
  - 診断完了時に answers + scores + resultIdx をlocalStorageに保存（最大10件保持）
  - 結果画面に「📜 診断履歴」タブを追加。診断日時・結果タイプ・復元ボタン表示
  - 「復元」で前回の診断結果を再確認可能。「削除」「全削除」で履歴を削除
- **M4: グラフ・スコア可視化（Chart.js）**: 診断結果を円グラフで視覚化
  - Chart.js (CDN v4.4.0) を読み込み
  - 結果画面に「📊 スコア分析」セクション追加。4つの結果タイプ（加湿・除湿・空気清浄・換気総合）のスコアを Doughnut chart で表示
  - グラフの色を結果タイプ別に変色（青・緑・黄・紫）。モバイル対応
- **M10: 印刷・結果エクスポート機能**: 診断結果を印刷・クリップボードコピー可能に
  - 結果画面に「🖨️ 印刷する」「📋 結果をコピー」ボタンを追加
  - 印刷: window.print() で PDF/紙出力対応
  - コピー: navigator.clipboard.writeText() でテキストをクリップボードへ格納
  - コピー後に「✅ コピーしました！」トースト通知を3秒表示

### v1.2.1 (2026-06-04)
- **P1: PWA theme-color 最適化**: HTML の theme-color タグを light/dark メディアクエリのみに統一（デフォルト重複削除）
- **manifest.json 拡張**: description フィールドを追加し PWA インストール情報を充実化

### v1.2.0 (2026-06-03)
- **PWA ダークモード対応**: theme-color をダークモードに動的対応（light: #6366F1、dark: #3730a3）
- **manifest.json 拡張**: screenshots・categories メタデータを追加し PWA 安定性向上
- **動的 theme-color 切り替え**: JS で OS ダークモード変更を監視し リアルタイム反映

### v1.1.0 (2026-05-29)
- 致命的バグ修正: JSコード重複宣言（SyntaxError）を解消し診断が正常動作するように修正
- ヘッダー・スタートカードの説明文切り捨て（「診...」）を完全表示に修正
- 診断中に「前に戻る」ボタンを追加（2問目以降）
- 診断結果にSNSシェアボタン（X/LINE）を追加（結果タイプ名付き）
- 診断結果を箇条書き表示＋推奨湿度バッジに変更（読みやすさ向上）
- 診断完了後に結果カードへ自動スクロール追加

## データ管理

- **ストレージ**: ブラウザ localStorage（外部API通信なし）
- **永続化**: ページ読み込み時に自動復元
- **クリア**: ブラウザのサイトデータ削除で初期化

## 技術スタック

- HTML5 / CSS3 / Vanilla JavaScript
- PWA対応（manifest.json / Service Worker）
- レスポンシブデザイン（モバイルファースト）

## 使い方

1. ページを開き、診断の説明を確認する
2. スタートボタンをクリックして診断を開始する
3. 表示される質問に順番に回答する（約1〜2分）
4. すべて回答すると診断結果が自動表示される
5. 結果のアドバイスを確認してSNSでシェアする

## よくある質問（FAQ）

**Q: 室内湿度・空気環境最適化診断は無料で使えますか？**

はい、完全無料でご利用いただけます。登録・ログイン・アプリのインストールも不要です。

**Q: 何回でも診断できますか？**

はい、何度でもご利用いただけます。条件を変えて繰り返し診断してみてください。

**Q: スマートフォンでも使えますか？**

はい、スマートフォン・タブレット・PCすべてのデバイスに対応しています。

**Q: 診断結果は保存されますか？**

結果はブラウザには保存されません。スクリーンショットやSNSシェアでご記録ください。

**Q: 入力したデータはサーバーに送信されますか？**

いいえ。すべての処理はブラウザ内で完結し、個人情報・入力データのサーバー送信は行いません。


## 関連サービス

- [家の断熱・省エネ診断](https://appadaycreator.com/home-energy-advisor/)
- [花粉症・アレルギー対策チェッカー](https://appadaycreator.com/pollen-allergy-checker/)
- [Sleep Quality Checker](https://appadaycreator.com/sleep-quality-checker/)

## テスト

| ファイル | フレームワーク | 概要 |
|---------|--------------|------|
| `tests/e2e/` | Playwright | 本番URL対象E2E（Jest対象外） |

## デプロイ

GitHub Pages（mainブランチ push → 自動デプロイ）

## ライセンス

MIT License
