# オンライン学習診断【無料】目的別スクール・サービス費用比較 - 技術仕様書

## 概要

**サービス名**: Online Learning Advisor
**バージョン**: 1.4.0（SEO・スキーマ強化・モバイルUI修正版）
**更新日**: 2026-06-19
**URL**: https://appadaycreator.com/online-learning-advisor/

学習目的・期間・予算から最適なオンライン学習サービスを無料診断。資格・スキルアップ・趣味対応。

## v1.4.0 改善内容（2026-06-19）

### P1改善案（実装済み）
1. **meta description 拡充（38文字→104文字）** ✅
   - 主要キーワード（Udemy・テックアカデミー・スタディサプリ等）を含む詳細説明に変更
   - og:description・twitter:description も同様に更新
   - 検索クリック率（CTR）改善・GSCインデックス促進を目的とする

2. **FAQPage schema 2問→5問へ拡充** ✅
   - サービス固有の具体的なFAQ5問をJSON-LDで実装
   - プログラミング月費用・転職スクール推薦・無料サービス案内など実用情報を追加
   - リッチスニペット獲得による検索表示改善を目的とする

3. **HowTo schema をサービス固有の手順に更新** ✅
   - 汎用的なステップ（「フォームに入力する」等）を学習目的選択→費用確認→無料体験の具体的な手順に変更
   - `totalTime: PT3M` を追加してユーザーの時間コストを明示

### P2改善案（実装済み）
4. **Cookie Banner モバイル表示修正（V1）** ✅
   - 600px以下でflex-directionをcolumnに変更し縦積みレイアウトへ
   - ボタンをwidth:100%にして押しやすさ改善
   - スマホでのテキストとボタン重なり問題を解消

## v1.3.0 改善内容（2026-06-10）

### P1改善案（実装済み）
1. **学習目標設定・進捗トラッキング機能** ✅
   - 診断結果画面に「🎯 学習目標を設定する」セクション追加
   - 目標達成期間を選択：3ヶ月・6ヶ月・1年・期限なし
   - localStorage に開始日・目標期間を保存
   - リピート訪問時に進捗率（経過日数/目標日数）を自動表示
   - 進捗バー・残り日数表示で継続モチベーション向上

### P2改善案（実装済み）
2. **FAQセクション重複削除・統一** ✅
   - 重複していた詳細形式（details タグ）のFAQセクションを削除
   - dl形式（用語説明）のFAQに統一・オンライン学習内容を特化
   - テキスト精度向上：不正確な「転職・キャリア」内容を削除
   - FAQ セクション5問→5問に整理

## v1.2.0 改善内容（2026-06-06）

### P1改善案（実装済み）
1. **コストグラフの数値パースバグ修正** ✅
   - カンマ含む数値（例: "1,200"）が "1" として扱われていた問題を修正
   - ラベルの全角括弧正規表現 `/（.*\)/` → `/（[^）]*）/g` に修正
   - 「無料」を明示的に 0 として処理

2. **リセット時URLハッシュクリア** ✅
   - `resetQuiz()` に `history.replaceState(null, '', location.pathname)` 追加
   - リセット後リロードしても前の結果が復元されないように修正

3. **AI・データサイエンスカテゴリ追加** ✅
   - Q1 に「🤖 AI・データサイエンス」選択肢追加
   - 対応結果: Udemy / Coursera / DataCamp を推薦
   - `calculateScores()` / `getResultKey()` / `scoreLabels` に対応ロジック追加

### P2改善案（実装済み）
4. **グラフ高さのモバイル対応** ✅
   - モバイル（640px未満）: 180px、PC: 240px に動的調整

5. **「もう一度診断する」ボタンの視認性向上** ✅
   - ボーダーのみ → 背景色付き（indigo-50）+ アイコン追加

## v1.2.0 改善内容（2026-06-06）

### P1改善案（実装済み）
1. **コストグラフの数値パースバグ修正** ✅
   - カンマ含む数値（例: "1,200"）が "1" として扱われていた問題を修正
   - ラベルの全角括弧正規表現 `/（.*\)/` → `/（[^）]*）/g` に修正
   - 「無料」を明示的に 0 として処理

2. **リセット時URLハッシュクリア** ✅
   - `resetQuiz()` に `history.replaceState(null, '', location.pathname)` 追加
   - リセット後リロードしても前の結果が復元されないように修正

3. **AI・データサイエンスカテゴリ追加** ✅
   - Q1 に「🤖 AI・データサイエンス」選択肢追加
   - 対応結果: Udemy / Coursera / DataCamp を推薦
   - `calculateScores()` / `getResultKey()` / `scoreLabels` に対応ロジック追加

### P2改善案（実装済み）
4. **グラフ高さのモバイル対応** ✅
   - モバイル（640px未満）: 180px、PC: 240px に動的調整

5. **「もう一度診断する」ボタンの視認性向上** ✅
   - ボーダーのみ → 背景色付き（indigo-50）+ アイコン追加

## フェーズ1改善内容（コード品質）（2026-06-05）

### P1改善案（実装済み）
1. **console.log削除** ✅
   - `loadResultFromHash()`内の開発用console.logを削除
   - エラーハンドリングはコメントで明記

## フェーズ2実装内容（2026-06-03）

### P1改善案（実装済み）
1. **診断結果の個別化・詳細化** ✅
   - スコア計算エンジン追加（`calculateScores()`）
   - 適性スコア表示セクション追加（0-100%の正規化スコア）
   - 上位3スタイルの適合度を視覚化（プログレスバー表示）

2. **紹介サービス数拡充** ✅ (部分実装)
   - 新カテゴリ追加：「プログラミング初心者向け」（Progate / ドットインストール）
   - 既存カテゴリに`alternatives`フィールド追加（代替案候補）

3. **費用比較の視覚化（テーブル→グラフ化）** ✅ (フェーズ2-改善版)
   - Chart.js を使用した横棒グラフで費用比較
   - サービス名と月額費用を視覚的に比較可能
   - レスポンシブ対応（モバイル～デスクトップ）

### P2改善案（実装済み）
4. **診断結果のURL共有機能（結果復元）** ✅ (新規実装)
   - 診断完了後に一意のURL（ハッシュ）を生成
   - `#r=encoded_answers` 形式で結果を復元
   - 「結果リンクをコピー」「Xでシェア」「LINEで送る」ボタン追加
   - URLから自動的に診断結果を復元表示

5. **SNS シェア最適化** ✅ (部分実装)
   - 動的メッセージ生成：診断結果を反映したテキスト
   - SNS シェアボタンの href を動的に更新
   - Twitter / LINE に対応

## データ管理

- **ストレージ**: ブラウザ localStorage（外部API通信なし）
- **永続化**: ページ読み込み時に自動復元
- **クリア**: ブラウザのサイトデータ削除で初期化

## 技術スタック

- HTML5 / CSS3 / Vanilla JavaScript
- PWA対応（manifest.json / Service Worker）
- レスポンシブデザイン（モバイルファースト）

## 使い方

1. ページを開き、診断の説明を確認する
2. スタートボタンをクリックして診断を開始する
3. 表示される質問に順番に回答する（約1〜2分）
4. すべて回答すると診断結果が自動表示される
5. 結果のアドバイスを確認してSNSでシェアする

## よくある質問（FAQ）

**Q: オンライン学習診断は無料で使えますか？**

はい、完全無料でご利用いただけます。登録・ログイン・アプリのインストールも不要です。

**Q: 何回でも診断できますか？**

はい、何度でもご利用いただけます。条件を変えて繰り返し診断してみてください。

**Q: スマートフォンでも使えますか？**

はい、スマートフォン・タブレット・PCすべてのデバイスに対応しています。

**Q: 診断結果は保存されますか？**

結果はブラウザには保存されません。スクリーンショットやSNSシェアでご記録ください。

**Q: 入力したデータはサーバーに送信されますか？**

いいえ。すべての処理はブラウザ内で完結し、個人情報・入力データのサーバー送信は行いません。


## 関連サービス

- [睡眠の質チェッカー](https://appadaycreator.com/sleep-quality-checker/)
- [BMI・体重管理](https://appadaycreator.com/bmi-body-tracker/)
- [家計簿診断](https://appadaycreator.com/household-budget-analyzer/)

## テスト

| ファイル | フレームワーク | 概要 |
|---------|--------------|------|
| `tests/e2e/` | Playwright | 本番URL対象E2E（Jest対象外） |

## デプロイ

GitHub Pages（mainブランチ push → 自動デプロイ）

## ライセンス

MIT License
