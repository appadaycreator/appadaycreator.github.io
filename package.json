{
  "name": "30service-tests",
  "version": "1.0.0",
  "description": "30サービスチャレンジ Playwright自動テスト",
  "scripts": {
    "test": "npx playwright test",
    "test:report": "npx playwright test --reporter=html",
    "test:single": "npx playwright test --grep",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  }
}
