# プログラミングスクール診断【無料】目的・予算別おすすめスクール - 技術仕様書

## 概要

**サービス名**: Programming School Advisor
**バージョン**: 1.5.2
**更新日**: 2026-06-20
**URL**: https://appadaycreator.com/programming-school-advisor/

目的・予算・学習スタイルから最適なプログラミングスクールを無料診断。未経験者・転職希望者対応。

## データ管理

- **ストレージ**: ブラウザ localStorage（外部API通信なし）
- **永続化**: ページ読み込み時に自動復元
- **クリア**: ブラウザのサイトデータ削除で初期化

## 技術スタック

- HTML5 / CSS3 / Vanilla JavaScript
- PWA対応（manifest.json / Service Worker）
- レスポンシブデザイン（モバイルファースト）

## 使い方

1. ページを開き、診断の説明を確認する
2. スタートボタンをクリックして診断を開始する
3. 表示される質問に順番に回答する（約1〜2分）
4. すべて回答すると診断結果が自動表示される
5. 結果のアドバイスを確認してSNSでシェアする

## よくある質問（FAQ）

**Q: プログラミングスクール診断は無料で使えますか？**

はい、完全無料でご利用いただけます。登録・ログイン・アプリのインストールも不要です。

**Q: 何回でも診断できますか？**

はい、何度でもご利用いただけます。条件を変えて繰り返し診断してみてください。

**Q: スマートフォンでも使えますか？**

はい、スマートフォン・タブレット・PCすべてのデバイスに対応しています。

**Q: 診断結果は保存されますか？**

結果はブラウザには保存されません。スクリーンショットやSNSシェアでご記録ください。

**Q: 入力したデータはサーバーに送信されますか？**

いいえ。すべての処理はブラウザ内で完結し、個人情報・入力データのサーバー送信は行いません。


## 関連サービス

- [睡眠の質チェッカー](https://appadaycreator.com/sleep-quality-checker/)
- [BMI・体重管理](https://appadaycreator.com/bmi-body-tracker/)
- [家計簿診断](https://appadaycreator.com/household-budget-analyzer/)

## テスト

| ファイル | フレームワーク | 概要 |
|---------|--------------|------|
| `tests/e2e/` | Playwright | 本番URL対象E2E（Jest対象外） |

## デプロイ

GitHub Pages（mainブランチ push → 自動デプロイ）

## ライセンス

MIT License

---

## 更新履歴

### v1.5.0（2026-06-13）

#### M7: 比較・シミュレーション機能（実装完了）
- **比較テーブルUI**: 4つの主要スクール（テックキャンプ・テックアカデミー・Progate・DMM WEBCAMP）を横並びで比較
  - 料金・学習期間・就職率・給付金を一覧表示
  - 推奨スクール（ユーザーの診断結果）を強調表示（黄色背景・左罫線）
  - タッチフレンドリーな表示でモバイル対応
  
- **シミュレーション機能**: 「もし〜だったら」の条件変更で即時再診断
  - 学習目的（転職・副業・スキルアップ・就活）をドロップダウン選択
  - 経験レベル（未経験・初級・中級）をドロップダウン選択
  - 予算（3万円以下・3〜10万円・10万円以上）をドロップダウン選択
  - 「条件で再診断」ボタンで即座に結果変更
  - シミュレーション結果を別セクションに表示

- **ビューモード切り替えUI**: 3つのボタンで表示モード切り替え
  - 📊 チャート表示：レーダーチャート + スキャッタープロット（既存）
  - 📋 比較表：全スクール横並び比較
  - 🔄 シミュレーション：条件入力 + 即時再診断
  - アクティブボタンを紫色、非アクティブをグレーで視覚的区別

- **実装技術詳細**：
  - `switchViewMode(mode)`: ビューモード切り替え、ボタンスタイル更新
  - `generateComparisonTable()`: 比較テーブル動的生成
  - `runSimulation()`: シミュレーション実行・結果表示
  - currentViewMode / currentSimulationResult グローバル変数で状態管理

#### GSC対策：構造化データ + キーワード強化
- **FAQPage schema拡張**: 5問から4問に集約し、より詳細な回答を設定
  - Q1: 無料診断について
  - Q2: 転職向け vs 副業向けスクール比較（新規）
  - Q3: スマートフォン対応について
  - Q4: 給付金対象スクール情報（新規・インデックス性向上）
  - Q5: 診断結果の信頼性（新規・CTA促進）
  
- **meta title改善**: 「転職・副業・給付金対応・5問で最適なスクール選び」を追加
  - 主要キーワード: プログラミングスクール診断、転職、副業、給付金、5問
  
- **meta description改善**: 学習目的別・給付金対応・主要6校名を明記
  - 主要キーワード: プログラミングスクール、転職、副業、給付金、テックキャンプ、テックアカデミー等
  - テキスト長: 160字以内（Google検索結果表示最適化）

#### 動作確認チェックリスト
- ✅ 結果セクションに3つの表示モード切り替えボタンが表示される
- ✅ 「📋 比較表」クリック → 4スクール横並び表示、推奨スクール強調
- ✅ 「🔄 シミュレーション」クリック → ドロップダウン3つ + 再診断ボタン表示
- ✅ シミュレーションで条件変更 → 「条件で再診断」で結果が即座に更新される
- ✅ ボタンのアクティブ状態（紫 vs グレー）が正しく切り替わる
- ✅ meta title・description に新キーワード含まれている（DevTools確認）

### v1.4.0（2026-06-10）

#### P1: 印刷・結果エクスポート機能（M10実装）
- 診断結果表示後、以下3つのボタンを追加：
  - **「📄 印刷する」**: window.print()でブラウザの印刷ダイアログを開く
  - **「📋 結果をコピー」**: 診断結果テキストをクリップボードにコピー（navigator.clipboard API）
  - **「📥 結果をダウンロード」**: 結果をテキストファイル（.txt）としてダウンロード
- 結果テキスト形式：
  ```
  【推奨スクール】[スクール名]
  
  🎯 理由:
  [理由テキスト]
  
  📌 特徴:
  • [特徴1]
  • [特徴2]
  ...
  
  生成日: [日時（YYYY-MM-DD HH:MM:SS）]
  ```
- 印刷時CSS（@media print）：既存ルールにより、ボタン・広告・CTAが自動非表示化

#### 実装技術詳細
- `generateResultText(key)`: 結果テキストを生成する関数（各スクール情報から自動生成）
- `printResult()`: window.print()を呼び出す
- `copyResultToClipboard()`: navigator.clipboard.writeText()でテキストをコピー、アラート表示
- `downloadResult()`: Blob + createObjectURL でテキストファイルをダウンロード

#### 動作確認チェックリスト
- ✅ 診断完了後、結果セクションに3つのエクスポートボタンが表示される
- ✅ 「📄 印刷する」をクリック → ブラウザの印刷ダイアログが開く（window.print()）
- ✅ 「📋 結果をコピー」をクリック → テキストがクリップボードにコピード（DevTools確認可）
- ✅ 「📥 結果をダウンロード」をクリック → テキストファイルがダウンロード開始
- ✅ 印刷プレビューで不要な広告・ボタンが非表示化されている

### v1.3.0（2026-06-10）

#### P1: localStorage による診断履歴・データ自動保存機能
- 診断回答・結果を localStorage に自動保存
- ページロード時に前回の診断結果を自動復元（ユーザー確認後）
- 直近5回分の診断履歴を「📋 診断履歴」セクションに表示
- 過去の診断結果をクリックで即座に復元表示可能
- 「履歴をクリアする」ボタンで全履歴削除可能

#### P2: Chart.js による結果の可視化
- レーダーチャート：5軸（料金、学習期間、就職率、メンター充実度、給付金）でスクール比較
  - 推奨スクール（太線・濃い色）と比較スクール3校（薄い線）を同時表示
  - ユーザーが各軸での強み弱みを視覚的に理解可能
- スキャッタープロット：料金 vs 学習期間でスクール配置図
  - X軸: 学習期間（月）、Y軸: 料金（万円）
  - 推奨スクールを大きく強調表示
  - コスパの良いスクール（左下）と集中学習スクール（左上）の位置関係を可視化

#### 実装技術詳細
- Chart.js v4.4.0 を CDN から読み込み（https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.js）
- schoolResults に以下フィールドを追加：
  - `radar`: { 料金, 学習期間, 就職率, メンター, 給付金 }（各項目0-100スコア）
  - `cost_万円`: 学習費用（数値）
  - `period_month`: 学習期間（月数）
  - `comparisonSchools`: 比較対象スクール3校（デフォルトは他3校）
- showResult() 関数に以下機能を追加：
  - `initializeCharts(key)`: レーダーチャート・スキャッタープロット初期化
  - localStorage 保存ロジック（quiz_answers, quiz_result, quiz_history）
  - 履歴セクション表示の更新
- 新規関数群：
  - `updateHistorySection()`: 履歴セクションの表示・更新
  - `restoreFromHistory(key)`: 過去の診断結果復元表示
  - `clearHistory()`: 履歴クリア

#### 改善効果
- **ユーザー体験向上**: 前回の診断結果が自動復元されるため、リピート利用時の操作削減
- **判断の説得力向上**: チャートでスクール比較が視覚的に理解しやすくなり、選択理由が明確化
- **データ永続化**: ブラウザ内 localStorage で完全にプライベート・セキュアに履歴管理

#### 動作確認チェックリスト
- ✅ ページロード時に localStorage から前回結果を読み込み、復元ダイアログを表示
- ✅ 診断完了後、回答と結果が localStorage に保存される（ブラウザ DevTools で確認可能）
- ✅ レーダーチャートが結果セクションに表示、推奨スクール（太線）と比較スクール（薄線）を区別表示
- ✅ スキャッタープロットで料金・期間の相対位置を2次元表示
- ✅ 履歴セクションに過去5件の診断結果をリスト表示
- ✅ 履歴のスクール名をクリックで結果が復元表示される
- ✅ 「履歴をクリアする」ボタンで全履歴削除可能

### v1.2.0（2026-06-06）

#### P1: 結果セクションHTMLバグ修正（重大）
- `<div id="result">` タグが壊れていた問題を修正（line 193の `<div ` が line 233の `id="result"...` と断片化していた）
- クイズ完了後に結果が正常表示されるよう修正
- ソーシャルプルーフ・楽天アフィリエイトブロックを result div 外の正しい位置に移動

#### P1: ソーシャルプルーフの口コミを正しい内容に修正
- 旧: 「子どもの英語学習サービス」「オンライン英会話」など英語学習サービスの口コミ（完全に誤り）
- 新: プログラミングスクール診断に関する口コミ3件（転職成功者・副業目指し・就活生の視点）

#### P1: FAQの「健康情報」誤記修正
- 「入力された健康情報はサーバーに送信・保存されません」→「回答情報はサーバーに送信・保存されません」に修正

#### P2: 推薦スクールに合わせた動的CTA実装
- `schoolResults` 各スクールに `ctas` フィールドを追加
- `showResult()` がスクール別CTAを動的に描画するよう変更
  - techcamp: テックキャンプ無料カウンセリング + Groovement Agent
  - techacademy: テックアカデミー無料体験 + SkillHacks
  - progate: Progate無料登録 + paiza無料登録
  - dmmwebcamp: DMM WEBCAMP無料説明会 + Groovement Agent



### v1.1.0（2026-06-04）

#### P1: スクール情報の充実化・給付金明記
- 各スクール情報に以下を追加：
  - **給付金対象フラグ**: 経産省認定の専門実践教育訓練給付金対象を明示
  - **実績数値**: 卒業生数、転職成功率、就職率などの具体的数値
    - テックキャンプ: 転職成功率99%、10,000名以上卒業
    - テックアカデミー: 副業月額平均19万円実績
    - Progate: 利用者数200万人以上、提携企業350社以上
    - DMM WEBCAMP: 就職率99%、転職成功3,500名以上
  - **給付金効果**: 最大70%OFF などの割引額を明記
  - **メンタリング内容**: 個別メンタリング、専任アドバイザーなどサポート内容を詳細化

#### P2: UX改善 - 質問の修正機能・モバイル最適化
- 前へボタン（「← 戻る」）追加：質問2以降で常時表示
  - 回答を修正したい場合、前の質問に戻って回答し直すことが可能に
  - 戻ると答え直した選択肢が反映されるフロー
- モバイルタップターゲット強化：
  - 選択肢ボタン: min-height 48px 以上を確保
  - 戻るボタン: min-height 44px 以上を確保（iOS推奨）
  - タッチデバイスでの誤タップ軽減

#### 改善効果
- ユーザーが自信のない質問を修正可能 → 診断精度向上
- スクール情報の信頼性向上（具体的数値・給付金情報）
- モバイルでのUIストレス軽減（ボタン拡大）
