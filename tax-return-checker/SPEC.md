# 確定申告 必要書類チェッカー｜申告要否判定と書類リスト自動生成 - 技術仕様書

## 概要

**サービス名**: Tax Return Checker
**バージョン**: 1.7.0
**更新日**: 2026-06-20
**URL**: https://appadaycreator.com/tax-return-checker/

収入タイプや状況から確定申告が必要か診断し、必要書類リストを自動生成。副業・医療費・住宅ローン・株式売買など8項目を入力するだけで判定できる無料ツール。

## データ管理

- **ストレージ**: ブラウザ localStorage（外部API通信なし）
- **永続化**: ページ読み込み時に自動復元
- **クリア**: ブラウザのサイトデータ削除で初期化

## 技術スタック

- HTML5 / CSS3 / Vanilla JavaScript
- PWA対応（manifest.json / Service Worker）
- レスポンシブデザイン（モバイルファースト）

## 使い方

1. ページを開き、入力フォームの項目を確認する
2. 必要な情報を入力または選択する
3. 実行ボタンをクリックして結果を取得する
4. 表示された結果・アドバイスを確認する
5. 必要に応じてコピー・SNSシェアで活用する

## よくある質問（FAQ）

**Q: 確定申告 必要書類チェッカー｜申告要否判定と書類リスト自動生成は無料で使えますか？**

はい、完全無料・登録不要でご利用いただけます。

**Q: 何回でも使えますか？**

はい、回数制限なく何度でもご利用いただけます。

**Q: 入力したデータはサーバーに送信されますか？**

いいえ。すべての処理はブラウザ内で完結し、入力内容はサーバーへ送信されません。

**Q: スマートフォンでも使えますか？**

はい、スマートフォン・タブレット・PCすべてに最適化されています。

**Q: 結果を保存・共有できますか？**

スクリーンショットでの保存またはSNSシェアボタンからご共有いただけます。


## 変更履歴

- **v1.7.0** (2026-06-20): M2 UX改善・V1〜V3視覚的問題修正・入力フォーム最適化
  - M2【コア機能UX改善】実装: 副業所得・医療費の入力フォームにラベル太字化（text-base）・ヘルプテキスト拡大・プレースホルダ簡潔化・入力欄パディング増加（px-4 py-3）で視認性向上
  - V2【進捗表示】改善: progress text サイズ拡大（text-sm→text-base）・絵文字付き表示（📍 質問 X / 8）で進捗認識を容易化
  - V3【モバイルレイアウト】最適化: quiz button mobile 時のサイズ縮小（min-height:48→52px）・フォント圧縮（0.8→0.75rem）・行高さ圧縮（1.3→1.2）でスクロール最小化

## 関連サービス

- [睡眠の質チェッカー](https://appadaycreator.com/sleep-quality-checker/)
- [BMI・体重管理](https://appadaycreator.com/bmi-body-tracker/)
- [家計簿診断](https://appadaycreator.com/household-budget-analyzer/)

## 実装済み改善（v1.6.0）

### P1改善: M2 コア機能のUX改善（入力UI・バリデーション）
- **リアルタイム数値フォーマット**: 入力中にカンマ区切り表示（例：150000 → 150,000円）
- **改善されたヘルプテキスト**: 
  - 副業：「20万円超：申告必須」「✓ 20万円以下：申告不要」などの色分けされたフィードバック
  - 医療費：「✓ 控除対象額：50,000円（150,000円 - 10万円）」の計算表示
- **エラーメッセージの強化**: より詳細で具体的なエラー内容（「負の数は入力できません」等）
- **スライダー・入力フィールドの連動**: 
  - スライダーで値を選択すると入力フィールドに自動反映
  - 入力フィールドの値がスライダーの範囲内なら自動で同期
- **ビジュアルフィードバック**: 
  - 右側に現在値をリアルタイム表示
  - エラーは赤色、成功は緑色、注意は通常色で表示
  - ボタンやテキストにホバー時の transition 効果
- **キーボードショートカット**: Enter キーで即座に値を確定できるように改善

## 実装済み改善（v1.5.0）

### P1改善: M10 結果のPDF/印刷出力機能
- **印刷ボタン追加**: 結果ページに「🖨️ 印刷」ボタンを追加
  - window.print() で ブラウザのネイティブ印刷機能を利用
  - PDF 保存・用紙への直接印刷が可能
  - 既存の CSS `@media print` で、ヘッダー/フッター/広告は非表示、結果のみ表示

### P2改善: M10 結果をCSV/JSONでダウンロード
- **CSV ダウンロード**: 判定・理由・必要書類を CSV 形式で出力
  - BOM 付きで、Excel でも日本語が正しく表示される
  - ファイル名: `tax-return-checker_結果_YYYY-MM-DD.csv`
- **JSON ダウンロード**: 構造化データとして出力
  - Pretty-print 形式で可読性確保
  - ファイル名: `tax-return-checker_結果_YYYY-MM-DD.json`
  - API 連携・データ分析ツール統合に対応
- **ダウンロード完了通知**: トースト表示で成功を確認

## 実装済み改善（v1.4.0）

### P1改善: M4グラフ・スコア・ゲージで結果可視化
- **Chart.jsライブラリ追加**: CDN から Chart.js 4.4.1 を読み込み
- **申告必要度ゲージ表示**: 結果ページに doughnut チャートでゲージを表示
  - 申告必須（スコア 100%）：赤色
  - 申告推奨（スコア 60%）：オレンジ色
  - 申告不要（スコア 20%）：緑色
- **スコア計算ロジック**: mustFile/shouldFile の判定に応じて 0-100 のスコアを自動計算
- **中央テキスト表示**: ゲージの中央にスコア数値とラベル（「申告必須」等）を表示

### P2改善: M6目標設定・達成度トラッキング
- **目標設定機能**: 結果ページに「来年の確定申告準備目標」セクションを追加
  - テキスト入力欄で目標を追加可能（例：医療費領収書を整理）
  - チェックボックスで達成状況を追跡
  - 削除ボタンで不要な目標を削除
- **localStorage 永続化**: 目標をブラウザに保存。ページ再読み込み時に復元
- **達成度表示**: 「達成度: X/Y (Z%)」で進捗を視覚化
  - プログレスバーで達成率をグラフィカルに表示
  - チェック時は自動保存・表示更新
- **最大表示数**: 目標が多い場合は max-height でスクロール対応

## 実装済み改善（v1.3.0）

### P1改善
- **ヘッダー視認性修正**: ヘッダー背景色を `#f0f9ff`（ライトブルー）から `#1e3a5f`（濃紺）に修正し、タイトル・サブタイトルが白文字で正しく表示されるようにした
- **FAQ誤記修正**: FAQ内の「健康情報」を「税務情報」に修正（他サービスからのコピーミス）
- **Q8ロジック追加**: 配偶者控除・扶養控除の回答（Q8）を `showResult()` に反映し、対応する書類を必要書類リストに追加するよう修正
- **申告不要時の書類表示修正**: 申告不要判定時はマイナンバー・印鑑等の共通書類を表示せず、代わりに「来年に備えるポイント」アドバイスを表示
- **copyResult バグ修正**: `querySelector('h3.text-xl font-bold')` の無効セレクタを `getElementById('result-judgment')` に修正。結果 HTML に `id="result-judgment"` / `id="result-reasons"` / `id="result-docs"` を付与

### P2改善
- **前の質問に戻るボタン**: Q2〜Q8 に「← 前の質問に戻る」ボタンを追加し、直前の質問に戻れるようにした
- **関連サービスリンク修正**: フッターの関連サービスを税金・家計系（家計簿診断・フリーランス税金アドバイザー・副業税金シミュレーター・ふるさと納税控除計算）に差し替え

## 実装済み改善（v1.2.0）

### P1改善
- **選択状態の記憶**: 各質問の選択肢を localStorage に保存。ページ再読み込み時に前回の選択を復元
- **高速リセット**: 「もう一度診断する」ボタンで location.reload() の代わりに DOM 操作で瞬時にリセット。回答・localStorage をクリア

### P2改善
- **診断履歴機能**: 診断完了時に日時・判定・理由・必要書類を JSON 形式で保存。結果画面に「診断履歴」セクションで最大10件表示
- **結果コピー機能（マークダウン）**: 判定・理由・必要書類をマークダウン形式でクリップボードにコピー。成功時にトースト「✓ コピーしました」を表示

## 旧改善（v1.1.0）
- チェックリスト連携機能
- モバイルUI最適化

## テスト

| ファイル | フレームワーク | 概要 |
|---------|--------------|------|
| `tests/e2e/` | Playwright | 本番URL対象E2E（Jest対象外） |

## デプロイ

GitHub Pages（mainブランチ push → 自動デプロイ）

## ライセンス

MIT License
